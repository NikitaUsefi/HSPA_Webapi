{
  "AppSettings": {
    
  },

  "ConnectionStrings": {
    //to use local sql with windows authentication
    //"Default": "server=localhost; database=hspa; integrated security=SSPI",
    "Default": "server=localhost; database=hspa; user id=sa;"
    //to use sql express
    //"Default": "server=localhost/sqlexpress; database=hspa; user id=sa; password:***"
    //to use local sql server with static username and password
    //"Default": "server=localhost; database=hspa; user id=sa; password:****"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*"
}
